import { StyleSheet } from 'react-native'

export const texts = StyleSheet.create({
  centerBold: {
    textAlign: 'center',
    fontWeight: 'bold'
  },
  textInput: {
    fontSize: 20,
    padding: 10,
    marginBottom: 10,
    borderWidth: 1,
    borderRadius: 4,
  },
})
